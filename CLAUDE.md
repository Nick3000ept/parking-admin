# CLAUDE.md — операционная шпаргалка

Контекст для будущих сессий Claude в этой папке. Не дублирует [TZ.md](TZ.md) — содержит только операционные детали разработки и деплоя.

## Проект

Веб-приложение для учёта работ на паркинге СБ3. Подробности в [TZ.md](TZ.md). Pre-deploy проверки — в [DEPLOY_CHECKLIST.md](DEPLOY_CHECKLIST.md).

**Состояние**: развёрнуто на боевом контуре, работает. Доступ для подрядчиков по токенам.

## Подключения

| Сущность | Значение |
|---|---|
| Spreadsheet ID | `1zB1IQJlla93txI9o-sH-mxD3CeeO0Ugkv8mHPDzcqiI` |
| Apps Script ID | `1gJBrX63FdnXLHuqX8jcAN57OVoV3l9uvTGUsYyq4bVrNfC3sf4tJ7apD` |
| GAS Web App URL (production) | `https://script.google.com/macros/s/AKfycbxX2skPihWXv4Wu8SV4Ehj_IybE4PjfzsnWG9b9dGjEwCtjkYev9WMEtQNicP0-N71xaA/exec` |
| Production deployment ID | `AKfycbxX2skPihWXv4Wu8SV4Ehj_IybE4PjfzsnWG9b9dGjEwCtjkYev9WMEtQNicP0-N71xaA` |
| GAS аккаунт (clasp) | `kuzkin@acons.group` |
| GitHub репо | `https://github.com/Nick3000ept/parking-admin` |
| GitHub Pages URL | `https://nick3000ept.github.io/parking-admin/` |

## Листы в Google Sheets

| Лист | Назначение |
|---|---|
| `Главный` | Реестр помещений (A–L) + парные колонки работ (M+). **A–L read-only для GAS** — `assertWritableColumn(col)` падает с ошибкой если col ≤ 12 |
| `Ведомость_работ` | Справочник работ: Полное название / Поверхность / Название для площадки / ИД_работы |
| `Ведомость_подрядчиков` | Подрядчик / ИД_подрядчик / Статус / Токен. Зарезервированные `ИД_подрядчик`: `admin` (роль Админ), `viewer` (роль Наблюдатель), `tech` (роль «Тех помещения») |
| `Типы_помещений` | Тип_помещения_детально / Тип_помещения_панель / Цвет. Связь с реестром через `Главный.Наименование = Типы_помещений.A` |
| `Журнал_монтажа` | История изменений процентов монтажа: Дата (настоящая дата+время) / Номер / Работа / Было % / Стало % / Кто. Пишется автоматически при каждом сохранении процентов (только реальные изменения); создаётся сам. Источник сводки «за 7/30 дней» в Аналитике (`?action=equipStats`). Ошибка журнала не ломает сохранение |
| `Комментарии_помещений` | Комментарии из карточки помещения: A Номер / B Комментарий / C Обновлено (дата+автор). Создаётся автоматически при первом комментарии. Пишет любой активный пользователь, кроме Наблюдателя (`setComment {num, text}`; пустой текст очищает; ввод экранируется `'` от формул, лимит 500 символов) |
| `Монтаж_оборудования` | Факт монтажа инженерного оборудования, **одна строка = одна работа в одном помещении** (405 строк / 121 помещение). A–F справочные: номер, корпус, этаж, наименование, **Работа**, **Подрядчик** (заполняет админ, можно разным работам разных); G — % готовности, H — статус (пишется бэком автоматически: 0 — Не начато, 1–99 — В работе, 100 — Готово), I — «Обновлено». Сайт пишет только G–I. Наборы работ по типам — `EQUIP_WORKSETS` в script.gs (электрощитовые 5, кроссовые 5, венткамеры 2, ИТП 2, ЦТП 3, узлы учёта 2, КНС 2, насосные 2, ТП 2; без наборов остались РУ, вводы КЛ, агрегатная и прочее — админ может дописать строки вручную). `?action=setupEquip` идемпотентен и **дозаполняет**: добавляет в конец листа недостающие пары (помещение, работа) из наборов, существующие строки не трогает; лист старой плоской структуры при первом запуске ушёл в `Монтаж_оборудования_архив` |

## Деплой

### Фронт (index.html)
```
cd "c:/Users/User/YandexDisk/VS_hub/СБ3_Паркинг_Админ"
git add index.html
git commit -m "..."
git push
```
GitHub Pages подхватит через ~1 минуту.

### Бэк (script.gs)
```
cd "c:/Users/User/YandexDisk/VS_hub/СБ3_Паркинг_Админ"
clasp push --force
clasp update-deployment --description "vN <что изменилось>" AKfycbxX2skPihWXv4Wu8SV4Ehj_IybE4PjfzsnWG9b9dGjEwCtjkYev9WMEtQNicP0-N71xaA
```
**Использую `update-deployment`, не `deploy`** — иначе создаётся НОВЫЙ deployment с новым URL, и фронт перестанет работать.

### Тестирование локально (без GitHub Pages)
```
cd "c:/Users/User/YandexDisk/VS_hub/СБ3_Паркинг_Админ"
python -m http.server 8000
```
Открыть `http://localhost:8000/?t=<токен>`.

## Тестовые токены

В лиcте `Ведомость_подрядчиков` (могут устареть, актуальные смотреть в Sheets):
- Овчинников: `81bc3299c7ceb6fb` (был в ТЗ как пример)
- Спектр: `ce3d0facb5187091`
- Альфа Строй: `8ca318c5521b5b66`
- Админ / Наблюдатель / Тех помещения (`tech`): создаются вручную в Sheets с произвольным токеном

## Архитектура коротко

- **Auth**: токен в URL `?t=` → localStorage → бэк проверяет на каждый запрос
- **doGet?action=load** возвращает весь снапшот (~1MB JSON: rooms, works, assignments, tip_panels)
- **doGet?action=ping** — диагностика, список всех листов в файле
- **doGet?action=equipStats** — сводка по монтажу: текущий срез (позиций/готово/в работе/не начато) + прогресс за 7 и 30 дней из Журнала_монтажа (позиций с прогрессом, доведено до 100%, прирост в процент-пунктах, разбивка по работам). Показывается блоком вверху окна «Аналитика», кэш на фронте 5 минут
- **doPost** — `setDate` / `clearDate` c hint `{row, colDate, colSp}` для fast-path; `setEquipBatch {items: [{num, work, pct}]}` — пакетная запись процентов монтажа (один Lock, одно чтение листа, запись группами RangeList по одинаковым значениям; лимит 500 позиций); `setEquip {num, work, pct}` — одиночная запись, оставлена для совместимости открытых сессий. Статус бэк вычисляет из процента сам. LockService только на запись.
- **Проценты монтажа копятся в очередь** (как отметки): изменения сразу видны в интерфейсе, в таблицу уходят по большой зелёной кнопке «Сохранить N», закреплённой снизу по центру экрана (решение пользователя 2026-08-24) — счётчик общий с отметками отделки. При ошибке несённое возвращается в очередь / откатывается построчно; закрытие вкладки с несохранённым — предупреждение браузера.
- **Монтаж оборудования в карточке**: блок в попапе у помещений, чьи работы есть в листе `Монтаж_оборудования` (load отдаёт `equip`: num → works[]). Строка блока = работа: название, подрядчик мелким шрифтом, поле процента (0–100); статус выводится цветом строки, «Обновлено» — в подсказке при наведении. Права построчные: Админ и роль «Тех помещения» — всё; подрядчик — строки, где он в колонке «Подрядчик»; Наблюдатель — просмотр. Optimistic UI с откатом, гонка ответов защищена `equipSeq`.
- **Кнопка «Тех помещения»** (в шапке, рядом с «Аналитикой», toggle с подсветкой) — **видна только роли «Тех помещения» (`tech`)**; у остальных ролей, включая Админа, скрыта (решение пользователя 2026-08-25). Роль `tech` видит все помещения, правит только проценты монтажа (по всем помещениям, как админ), отметки отделки бэк ей отклоняет. Кнопка включает отдельный режим: показываются только помещения группы «Тех помещения»; на карточке — конкретное наименование и **работы монтажа с полями процентов прямо на карточке** (без открытия попапа; карточка шире, класс `.card.tech`); вместо чипсов работ в шапке — **категории тех-помещений** (Венткамеры / Электрощитовые / Кроссовые / ИТП / ЦТП / Узлы учёта / КНС / Насосные / ТП / РУ / Вводы КЛ / Прочие — классификатор `roomTechCategory()` по подстроке наименования, single-select). Галочки/зелёный по отделке в этом режиме на карточках не показываются. При входе в режим сбрасывается выбранная работа, при выходе — выбранная категория.
- **Optimistic UI**: клик в режиме отметки моментально красит карточку, fetch уходит параллельно. Race condition защищён через `cellSeq[num|idRaboty]`.
- **Кэш**: `localStorage.parking_data_<token>` с `version_hash` — мгновенный рендер при перезагрузке + фоновое обновление.

## Известные нюансы

- **Параметр токена различается**: на сайте — `?t=<токен>`, а в прямых запросах к GAS API —
  `?token=<токен>` (напр. `?action=load&token=...`). С `t=` бэк отвечает «Доступ закрыт»,
  что легко принять за устаревший токен.
- При переименовании листов в Sheets — синхронно поменять `CONFIG.SHEET_*` в `script.gs` и `clasp push`.
- Чтобы добавить новую работу: строка в `Ведомость_работ` + две колонки в `Главный`. UI подхватит при F5.
- Чтобы добавить подрядчика: строка в `Ведомость_подрядчиков` со свежим токеном. Имя автоматом попадёт в Data Validation колонок `*СП` (если он настроен).
- Зелёный цвет НЕЛЬЗЯ использовать в `Типы_помещений.Цвет` — он зарезервирован за «работа готова».
- На бэке имена сравниваются после `.trim()` — защита от trailing-пробелов в Sheets.
- **Даты отметок в «Главный» живут в двух форматах**: настоящие даты (бэк отдаёт `yyyy-MM-dd`)
  и текст `дд.мм.гггг` (~3,5 тыс. ячеек, внесены вручную до/мимо сайта — бэк отдаёт строку
  как есть). Всё, что читает даты на фронте, должно понимать оба формата — хелпер
  `dateToYm()` в index.html. Инцидент 2026-08-11: «Аналитика» считала «Готово» только по
  ISO-датам и показывала 606 м² стяжки вместо ~13 000.
- **НЕ писать тестовые/диагностические значения в дальние пустые ячейки листа «Главный»**
  (напр. столбец 200). Любое значение в дальнем столбце/строке раздувает
  `getLastColumn()`/`getLastRow()`, и после этого `loadSnapshot` (`getDataRange`) и
  `setMarks` (читает весь лист `getRange(1,1,lastRow,lastColumn)`) сканируют раздутый
  диапазон — load и каждое сохранение резко тормозят, вплоть до «Failed to fetch» на
  фронте (браузер обрывает медленный запрос). Реальные данные — колонки A..AD (1..30).
  Для диагностики использовать **отдельный временный лист** или ячейку рядом с данными,
  и **обязательно чистить след** после теста. Симптом раздутия виден в `?action=ping`:
  поле `glavny_info.cols` должно быть ~30, не больше. (инцидент 2026-07-23→24: метка
  `DBG_...` в `GR1` раздула лист до 200 столбцов и затормозила сохранение.)

## Что сделано в текущей итерации (history)

- ✅ Базовый каркас + GAS подключение
- ✅ Матрица этаж × корпус с карточками
- ✅ Цветовая индикация по типу помещения
- ✅ Авторизация по токену, очистка URL после первого входа
- ✅ Четыре роли: Подрядчик / Админ / Наблюдатель (`viewer`) / Тех помещения (`tech`, 2026-08-25)
- ✅ Optimistic UI + fast-path GAS + sequence для race condition
- ✅ Узкий LockService только на write
- ✅ Мультивыбор Ctrl+Click, нижняя панель с площадью и тегами
- ✅ Режим отметки (зелёная шапка, toggle всей карточкой)
- ✅ Прогресс в шапке: помещений / площадь, с цветовой разбивкой готово/остаток
- ✅ Чипсы работ над матрицей, single-select, сгруппированы по поверхности
- ✅ Тёмная панель фильтров (одного цвета с шапкой)
- ✅ GitHub Pages деплой через `git push`

## Что НЕ автоматизировано (намеренно)

- Авто-деплой GAS через GitHub Actions — лишняя инфраструктура; `clasp push` локально достаточно
- Unit-тесты GAS — слабый testing framework в Apps Script; полагаемся на smoke-test через ping
- CI/CD pipeline — `git push` → Pages обновляется самостоятельно
