# CLAUDE.md — операционная шпаргалка

Контекст для будущих сессий Claude в этой папке. Не дублирует [TZ.md](TZ.md) — содержит только операционные детали разработки и деплоя.

## Что это за проект

Веб-приложение для учёта работ по паркингу СБ3. Подробности — в [TZ.md](TZ.md).

## Google Sheets

- **ID таблицы**: `1zB1IQJlla93txI9o-sH-mxD3CeeO0Ugkv8mHPDzcqiI`
- **URL**: https://docs.google.com/spreadsheets/d/1zB1IQJlla93txI9o-sH-mxD3CeeO0Ugkv8mHPDzcqiI/edit
- **Решение**: работаем напрямую на исходном листе с реестром (добавляем колонки работ туда же). Отдельной проектной копии нет.
- **Бэкап**: раз в неделю вручную через `Файл → Создать копию` — критично, потому что работаем на боевых данных
- **Листы**:
  - `Главный` — реестр помещений (A–L) + колонки работ и СП (M+). **Реестровая часть A–L read-only для GAS** (никогда не писать туда из кода).
  - `Работы` — справочник работ (4 колонки)
  - `Подрядчики` — справочник пользователей (4 колонки), включая запись `Админ`
  - `Типы помещений` — соответствие тип → панельная категория → цвет

## GAS deployment

- **Веб-приложение**: _<URL не задан, появится после первого деплоя>_
- **Execute as**: Me (workcacc2025@gmail.com)
- **Who has access**: Anyone (защита через токен)
- **Правило**: при правке `script.gs` использовать `Deploy → Manage deployments → Edit (карандаш) → New version` — НЕ создавать `New deployment` (это меняет URL)

## GitHub

- **Репозиторий**: parking-admin на github.com/Nick3000ept _(будет создан)_
- **Хостинг**: GitHub Pages, ветка `main`, корень
- **URL после деплоя**: https://nick3000ept.github.io/parking-admin/

## Workflow разработки

1. Изменения в `index.html` → коммит → push → автодеплой Pages (~1 мин)
2. Изменения в `script.gs` → ручное копирование в Apps Script Editor → Edit deployment → New version
3. Перед каждым деплоем — пройти по `DEPLOY_CHECKLIST.md` раздел A

## Известные грабли (заполняется по мере)

_(пусто пока)_

## Тестовые токены

- _Тест-токены не выделены. Используем боевые токены подрядчиков из листа `Подрядчики` для smoke-тестов._
- При тестировании админских функций — использовать админский токен с `ИД_подрядчик=admin`.
